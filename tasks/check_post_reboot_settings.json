{
  "description": "Validates that post-reboot settings match expectations.\n\nIncludes FIPS and SELinux modes, Puppet Server and PuppetDB status, and whether the expected ports are listening",
  "input_method": "stdin",
  "parameters": {
    "simp_config_file": {
      "description": "Path to simp_conf.yaml file that was used to run `simp config -a`",
      "type": "Stdlib::Absolutepath"
    },
    "simp_config_settings_file": {
      "description": "Path to simp_config_settings.yaml Hiera file (generated by `simp config`)",
      "type": "Stdlib::Absolutepath"
    }
  },
  "files": [ "ruby_task_helper/files/task_helper.rb" ]
}
