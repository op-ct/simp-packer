---
config:
  transport: ssh
  ssh:
    host-key-check: false
    run-as: root
    #tmpdir: '/var/local/simp'
    connect-timeout: 60
    interpreters:
      rb: /opt/puppetlabs/puppet/bin/ruby

features:
  - puppet-agent
