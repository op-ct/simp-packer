---
config:
  transport: ssh
  ssh:
    host-key-check: false
    run-as: root
    #tmpdir: '/var/local/simp'
    connect-timeout: 60
    interpreters:
      rb: /opt/puppetlabs/puppet/bin/ruby
  local:
    tmpdir:
     _plugin: env_var
     var: PWD

features:
  - puppet-agent
